server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # Tenta carregar o arquivo, se não encontrar direciona para o index.html (SPA)
        try_files $uri $uri/ /index.html;
    }
}
